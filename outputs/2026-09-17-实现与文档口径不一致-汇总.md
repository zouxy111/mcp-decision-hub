# 实现 vs 文档 · 口径不一致汇总（一次性梳清）

日期：2026-09-17 ｜ 触发：`get_digest` 一处对不上，要求同类问题一次查全

**核查方法**：逐接口把「文档规定的口径」与「代码实际行为」并排对，两边都带
`文件:行号`。文档侧覆盖 PRD v1.2 / 09-14 裁决答复 / 09-16 裁定更正与补定 /
开工包 10 个 PRD / 交接稿 / roR8Pk 隐私评估；实现侧覆盖 `hub/mcp_server/`、
`hub/api/`、`hub/schemas/`、`hub/web/routes_*.py`、`hub/db/`。
**未做**：变异检验、`hub.db` 指纹（本件不改码，无需）。

> **怎么用**：每条最后都有「建议」。你只需对每条回「改」或「维持」，
> 或者直接说「按建议执行」。**A/B 两组是真的代码对不上文档，D/F 组是文档自身
> 有问题，E 组是可以直接清的陈旧文案。**

---

# A 组 · 接口出参真的与文档不符（要动代码）

## A1. `get_digest` 缺了「最新决议 / 决议草案」这一节 ★你点名的那个

**① 文档原文与上下文**

- `outputs/2026-09-14-裁决答复-致执行侧.md:25-28`（§一-3 get_digest 口径，owner 定）：
  > ### 3. get_digest 口径 → **一页纸现状**（采纳推荐）
  > 返回内容 = **当前轮次 + 最新结论/决议草案 + 未决开口（卡着什么）**。
  > - **不含**逐人立场明细（与裁决 4 的收口方向一致）。
- `outputs/2026-09-16-裁定更正与补定-致执行侧.md:110`（口径总表）：
  > | `get_digest` 口径 | 一页纸现状 | **不变** | 无 |
- `outputs/2026-09-14-开工包/03-get_digest.md:15-18`（唯一给出键名的地方）：
  > `current_round`：当前轮次号与状态
  > `decision`：最新决议（已定稿）或决议草案摘要（`pending_review` 时给草案，标注状态）
  > `open_items`：未决开口清单（来源：本轮立场的 `open_questions` 去重聚合 + 僵持/阻塞原因）

**② 当前实现实际返回**（`hub/mcp_server/methods.py:806-840`，`mcp_get_digest`）

```python
return {
    "matter_id": matter_id,
    "status": matter.status,
    "round_number": round_number,
    **_summary_payload(summary),   # consensus_points / divergences /
}                                  # blind_spots / open_questions / convergence
```

八个键：`matter_id` / `status` / `round_number` / `consensus_points` /
`divergences` / `blind_spots` / `open_questions` / `convergence`。

**③ 不一致的具体表现**

- **`decision` 整节不存在** —— Agent 拿 digest 时**看不到「现在有没有决议、决议是
  什么状态」**，而这恰恰是「这个事项现在进行到哪了」最该回答的问题。已定稿 /
  待审的决议在 digest 里完全不可见（决议数据在 `resolutions` 表，digest 没读它）。
- 键名与文档也不同：文档要 `current_round`，实现给 `round_number`（且只给数字，
  不带「状态」）；文档要 `open_items`，实现给 `open_questions`（直接从摘要搬运，
  没有「去重聚合」也没有「僵持/阻塞原因」）。
- 实现反而**多给了**三个键：`consensus_points` / `divergences` / `blind_spots` ——
  文档没要求，属自行扩面。

**④ 原因**

`873ff8d` 的提交说明自述：`- 3: mcp_get_digest 返回最新 ok 摘要+状态+收敛（简单形态）`。
即**落地时自行把口径简化成「简单形态」**，但 09-14 答复与 09-16 更正件都**没有**
改口径（09-16 那格还专门写了「**不变**」）。所以这是**实现单方面偏离**，不是口径变更。

**⑤ 建议**

**改。** 保留现有五个摘要字段（它们无害且有用），**补上 `decision` 一节**，
把 `round_number` 改名或补一个 `current_round` 结构。注意：`decision` 一节按
PRD-03 只给**摘要 + 状态**，不含 `user_id` / `confidence`。

---

## A2. `get_digest` 没有 Pydantic 契约 —— 全仓唯一一个裸返回 dict 的对外函数

**① 文档原文**

- `outputs/2026-09-14-开工包/03-get_digest.md:19`：
  > 2. 出参 Pydantic 契约（`DigestOut`，`_Strict` 惯例）
- 同文 `:50`（验收第 2 条）：
  > 2. 出参字段白名单被契约钉死（手工塞一个 `user_id` 进去必须炸）
- `docs/superpowers/specs/2026-09-13-mcp-decision-hub-prd-v1.2.md:430-437`（§9.6.1）：
  > 对外可调用契约限定为**两组**，且全部以 Pydantic 模型声明，**不得裸返回 `dict`**

**② 当前实现**

`hub/schemas/mcp_outputs.py` 里**没有 `DigestOut`**（全仓搜 `DigestOut` 0 命中）。
`mcp_get_digest` 直接 `return {...}`，没走 `_contract`。

实测：`hub/mcp_server/methods.py` 里 `return {` 共 5 处，其中 4 处是内部装配辅助
（`_summary_payload:457` / `_previous_summary_view:483` / `_resolution_view:498` /
`_item_brief:533`），**只有 `:838`（`mcp_get_digest`）是对外函数裸返回**。
全仓其余 10 个 `mcp_*` 函数全部经 `_contract`。

**③ 不一致的表现**

字段漂移（改名 / 丢字段 / 冒出新字段）在这里**不会被拦住**，与 §9.6.1 的硬约束
和 PRD-03 的验收第 2 条都相冲。其余工具出错是「产出时报错」，它是「带病出门」。

**④ 原因**

同 A1：`873ff8d` 落地时按「简单形态」走，跳过了契约这一层。后续没人补。

**⑤ 建议**

**改，且与 A1 一起做。** 加 `DigestOut(_Strict)`，字段与最终形状一致；
`mcp_get_digest` 改走 `_contract`。验收第 2 条（塞 `user_id` 进去必须炸）顺手补一条测试。

---

## A3. `StanceRead` 仍直出 `confidence` 原值 —— PRD-07 整块没落地

**① 文档原文**

- `outputs/2026-09-14-开工包/07-confidence分档外发.md:3-18`：
  > `confidence` 原值（float）目前直出到 API……且「0.7 硬阻断」语义公开……
  > 组合起来可反推「谁在卡结论」。本块把外发粒度从原值收窄为**分档**。
  > 1. 分档纯函数…… `def confidence_band(confidence: float) -> str  # "low"|"medium"|"high"`
  >    档位边界建议：`[0, 0.4)` low / `[0.4, 0.8)` medium / `[0.8, 1.0]` high
  > 2. `StanceRead` 单读出参：`confidence` 原值**移除**，替换为 `confidence_band`
  > 3. 数据库存储**不动**
- `outputs/2026-09-14-裁决答复-致执行侧.md:89`：
  > | roR8Pk 第 2 条（confidence 分档外发） | ✅ 按私有字段清单联动开工 |

**② 当前实现**

- `hub/schemas/stance.py:96`：`confidence: float` —— **原值直出**。
- 全仓**无 `confidence_band`**、无分档纯函数（搜 `confidence_band` 0 命中）。
- 对照：列表端点 `StanceListItem`（`:153-178`）**已正确裁掉** confidence 等 5 个私有字段。
  即「列表已收，单读没收」。

**③ 不一致的表现**

单读端点（`GET /api/items/{id}/stances/{user_id}`）仍能拿到原值 float。配合
`hub/domain/convergence_eval.py` 里公开的 0.7 硬阻断语义，**仍可反推「谁在卡结论」**——
正是这条口径要堵的洞。09-14 裁决把它标成「✅ 按私有字段清单联动开工」，
但代码里一行都没有。

**④ 原因**

PRD-07 的「文件所有权」写「**⚠️ 与 PRD-04 同文件 → 串行**」。PRD-04 后被
**整体作废**（k=3 方案被推翻），PRD-07 很可能因此被一起搁置 —— 但 README 明确写
「其余块（02/03/06/07/08/09/10）**未受本次裁定影响**」，所以它**应该做、没做**。

**⑤ 建议**

**你这个要单独拍。** 三种选法：
- 改：按 PRD-07 做（加 `confidence_band`、`StanceRead` 去掉原值）。
- 维持 + 改文档：承认单读保留原值（例如认为「有权读的人本就该看到依据」），
  去把 PRD-07 标成作废并把 09-14 裁决那格更正。
- 维持 + 不改文档：那就留一条已知未达成项，交付报告里挂着。

**我的看法：最好是「改」。** 因为「推导谁在卡结论」这件事，单读端点的受众
恰好就是能看到别人立场的那批人，洞最大。

---

# B 组 · 留痕与字段白名单

## B1. 读取留痕的 detail 多了 `content_hash` 一个键

**① 文档原文**

- `outputs/2026-09-14-开工包/06-rsEXuh-留痕.md:10-11`：
  > detail = `{viewer_user_id, fact_version}`（**只记这两个键**，不记视图内容）
- 同文 `:39`（验收片 1）：
  > 调一次 analysis → `audit_events` 恰多一条 `audience_view_delivered`，
  > detail 恰为 `{viewer_user_id, fact_version}` **两键**
- `docs/superpowers/specs/2026-09-13-执行到交付-PRD交接稿.md:256`：
  > ⚠️ `audit.py` 中**无合适常量** → 需**新增** `AUDIENCE_VIEW_DELIVERED`
  > （`detail={viewer_user_id, fact_version}`）【待 owner 确认常量名】
- 同稿 `:807`（§11.3 #6）已把它列为已发现的偏差：
  > | 6 | §4 B1 | 留痕 detail = `{viewer_user_id, fact_version}` | 实际含 `content_hash`（`stances.py:220-222`） |

**② 当前实现**（`hub/api/stances.py:212-224`）

```python
audit.record_audit(
    session, audit.AUDIENCE_VIEW_DELIVERED,
    actor_user_id=user.id, matter_id=matter_id,
    detail={
        "viewer_user_id": user.id,
        "fact_version": fact.fact_version,
        "content_hash": compute_stance_content_hash(...),   # ← 第三个键
    },
)
```

**③ 不一致的表现**

detail 是**三个**键，文档要**两个**。语义上 `content_hash` 不泄露立场正文
（它只是「交付了哪个版本的视图」的哈希），所以**不是隐私问题**，是**契约问题**：
文档把 detail 键集合定义为可钉死的最小面，实现多给了一个。

**④ 原因**

`content_hash` 是实现时自行加的（同一个 `compute_stance_content_hash` 在别处也用于
「交付了哪个版本」的校验）。文档没跟上，也没人回头改。

**⑤ 建议**

**维持实现 + 改文档**（我倾向这个）：多记一个「交付了哪个内容版本」对事后审计**更有用**
（能证明「当时给他的就是这一版」），且不泄露正文。改法：把 PRD-06 `:10-11` 与 `:39`
的两键改成三键，交接稿 §11.3 #6 那行标成「已按实现更正」。
**若你要严格守两键**，那就删掉 `content_hash` —— 但会丢掉「交付版本可追溯」这条能力。

## B2. 文档承诺的「钉死 detail 键集合」的测试**不存在**

**① 文档原文**（同上一处）

- `06-rsEXuh-留痕.md:45`：
  > 2. detail 键集合被测试钉死（**多一个键就红**）

**② 当前实现**

全仓在 `tests/` 下搜 `audience_view_delivered` / `AUDIENCE_VIEW_DELIVERED` →
**0 命中**。也就是说：**这条验收测试从来没写过**，而它正是唯一能拦住 B1 的东西。

**③ 不一致的表现**

文档承诺了一道闸门，闸门不存在。B1 之所以能悄悄发生，就是因为它。
（顺带：`hub/api/audit.py:43` 常量已批准、`:215` 调用已落地，**只有测试缺位**。）

**④ 原因**

06 这一块的验收第 1 条（「恰多一条审计 + detail 两键」）和常量落地一起做了，
键集合那条被漏掉。

**⑤ 建议**

**补测试。** 与 B1 的取舍绑定：若维持三键，测试就钉三键；若回到两键，就钉两键。
不管选哪个，**这条测试必须有** —— 否则下次还会漂。

---

# C 组 · 权限口径

## C1. 「非成员」在两层返回不同状态码（实现各自都合规，但整体分叉）

**① 文档原文**

- 流水线侧 —— `docs/.../prd-v1.2.md:483`（§9.5 错误码表）：
  > | 403 | `FORBIDDEN_SCOPE` | Token 对应用户无权访问目标任务或事项 |
  - 同文 `:649`（场景 25）：
  > 未勾选时发起人无任务但 `get_matter_status` 仍返回 `200`（**不得 `403`**）
- 立场层侧 —— 同文 `:579`：
  > 非参与人、非事项成员一律 `404 RESOURCE_NOT_FOUND`，且文案与「事项不存在」一致，
  > **不泄露事项是否存在**。

**② 当前实现**

- `hub/mcp_server/methods.py:393-406`（`mcp_get_matter_status`）：非成员 →
  写 `FORBIDDEN_DENIED` 审计 + **403**。
- `hub/api/stances.py:246`（`_require_matter_access`）：非成员 → **404**「事项不存在」。

即：**两条都照各自文档做了**。

**③ 不一致的表现**

同一个问题「我是不是这件事的人」，在**流水线层**得到 403（等于**确认**了「这件事存在、
但你没份」），在**立场层**得到 404（**不确认**）。立场层之所以要 404，是隐私评估的
明确要求；流水线层没跟着改。

**④ 原因**

两层是不同时期、不同裁决下的产物：流水线层沿用 v1.1 的 403 口径，立场层的 404
是 roR8Pk 隐私评估后才立的。**没有一份文档同时管这两层**，所以分叉一直没人发现。

**⑤ 建议**

**维持现状 + 补一句文档**（我倾向）：把流水线层的 403 改成 404 会动到既有测试与
`get_matter_status` 的场景 25 口径，成本不小；而「事项标题是否可见」对发起人/参与人
之外的令牌持有者意义有限。**但必须在 PRD 里写明这处分叉是有意的**，否则
后人读到两处矛盾必然来「修」一个。

---

## C2. `get_summary` 的成员闸门是**我 09-16 加的**，文档没要求（实现比文档严）

**① 文档原文**

- `docs/.../prd-v1.2.md:579` 那段「一律 404」讲的是**提交**前提，不是读。
- `hub/mcp_server/methods.py:600` 原 docstring 只写：
  > `get_summary`：该事项最新一轮 ok 摘要（五字段无身份）。
- 我翻遍交接稿与开工包：**`get_summary` 的读权限从未被规定**。

**② 当前实现**（`methods.py:598-612`，09-16 我改的）

```python
"""get_summary：该事项最新一轮 ok 摘要（五字段无身份）。

读侧闸门：事项成员（发起人 ∪ 参与人）可见；**非成员一律 404**……
"""
_require_matter(session, matter_id=matter_id, user_id=user_id)
```

**③ 不一致的表现**

**实现比文档严。** 原实现**完全没有闸门**（任何持令牌用户可读任意事项摘要，是真漏洞，
已在 09-16 修掉）。修完之后，实现的门槛**高于**任何文档要求 —— 文档对这件事**沉默**。

**④ 原因**

09-16 我给 `get_summary` 挂 REST 端点时发现它没闸门。不补闸门等于**新开一个无鉴权
读口**，所以补了；但当时只把理由写进代码注释，**没回填文档**。

**⑤ 建议**

**维持实现 + 回填文档。** 在 PRD 或交接稿里补一句「`get_summary` 读侧 = 事项成员，
非成员 404」，与立场层其余读口同口径。**不要放宽回去。**

---

# D 组 · 文档自相矛盾，实现已选一支（要回填文档，不动代码）

## D1. `content_digest` 不匹配该返回哪个错误码

- `docs/.../prd-v1.2.md:609`（§15.1 字段表）：
  > 不匹配返回 `422 VALIDATION_FAILED`（摘要不匹配）
- 同文 `:388`（§9.3）与 `:412`（§9.5）：
  > 与 `content_digest` 不一致时返回 `422 HUMAN_APPROVAL_REQUIRED`
- 同文 `:463`（场景 32 附带）：
  > `content_digest` / `content_hash` 与正文不匹配时**两条路径均返回 `422 HUMAN_APPROVAL_REQUIRED`**

**实现**：`hub/mcp_server/methods.py:289` →
`ApiError(422, "HUMAN_APPROVAL_REQUIRED", "content_digest 与提交正文不匹配（摘要不匹配）")`。

**即：实现跟 §9.3/§9.5/场景 32 一致，跟 §15.1 不一致。** §15.1 是那张表里的孤立笔误。

**建议：改 §15.1**（把 `VALIDATION_FAILED` 改成 `HUMAN_APPROVAL_REQUIRED`）。代码不动。

---

## D2. FR-20「Agent 无拍板权」至今没改，且 PRD 里那处指引是**悬空引用**

- `docs/.../prd-v1.2.md:236`（FR-20，v1.1 原文）：
  > | FR-20 | 仅发起人可以拍板…… | P0 | 参与人**和 Agent 无拍板权限**；未拍板前事项不能进入 `completed` |
- `outputs/2026-09-14-裁决答复-致执行侧.md:19-23`：
  > - 开通 `can_commit` 后，Agent 经 `decide_item` **可对普通事项直接终裁**。
  > - ⚠️ **这是对 PRD FR-20「Agent 无拍板权」的正式修订**：`can_commit` 成为显式例外。
  >   PRD v1.2 落库稿需同步修订该条及 FR-12b/12c 相关表述。
- `outputs/2026-09-14-开工包/02-decide_item.md:15`：
  > ## ⚠️ 硬顺序：先改 PRD，再写代码

**实现**：`decide_item` 已按修订版做了（`resolutions.decide_resolution` 带
irreversible 403 守卫 + 仅发起人守卫）。

**不一致的表现（两条）**：
1. **FR-20 正文没改**（开工包自己要求的「先改 PRD」这一步没做）。
2. PRD 里唯一的指引是 `:684` 那句「**FR-20 语义修订 → 见 §2.4**」，但
   **§2.4（`:140-175`）里根本没有 decide_item 的内容** —— `decide_item` 全文只在
   `:684` 出现过一次。**这是一处悬空引用**，照着找会找不到。

**建议：改文档。** 把 FR-20 按裁决修订，并把 `:684` 的指引指到真正写着的地方
（现在应该指 09-14 裁决答复 §一-2）。代码不动。

---

## D3. PRD §9.6.1「契约边界 = 4 端点 + 4 工具」与事实（12 端点 + 11 工具）严重不符

- `docs/.../prd-v1.2.md:430-437`：
  > 对外可调用契约限定为**两组**…… 1. **JSON API 4 个端点**（全部是 `/api/items/*/stances`）
  > 2. **MCP 4 个工具返回值**：`list_pending_tasks`、`get_task`、`submit_output`、`get_matter_status`

**事实**：JSON 端点 **12 个**（`/api/agent/*` 4 个 + `/api/items/*` 8 个）；
MCP 工具 **11 个**（流水线 4 + 立场层 7）。

**不一致的表现**：§9.6.1 写这份时立场层还没长出来，之后没人更新。现在照它读会得出
「立场层那 6 个工具不需要契约模型」的错误结论 —— **A2（get_digest 没契约）很可能
就是这么漏掉的**：它不在 §9.6.1 的名单里。

**建议：改文档。** 把边界改成事实（并列清单），并补一句「新增对外接口必须同步登记此处」。
代码不动。

---

## D4. 交接稿 §11.4 两处「待定口径」已过期

- `docs/.../2026-09-13-执行到交付-PRD交接稿.md:818-822`：
  > ### 11.4 两处待定口径（非工程细节，需 owner / lead 定）
  > 1. **`rUdiTJ` 事项间自相矛盾**……建议以 `r5Am9i` 为准更正 `rUdiTJ`。**属产品口径，未擅动。**
  > 2. **`rs9ncY` 完成标准缺位**：原文只有「做什么」4 条 + 待定项 → 须先补定。

**事实**：
1. `rUdiTJ` 的冲突，09-14 裁决答复 `:23` 已给出口径解释；09-16 owner 又拍板
   `can_commit` 开放。**只剩「回填事项正文」的动作没做**（需看板工具，当前不可达）。
2. `rs9ncY` 的完成标准 **已经找到**：开工包 `09-rs9ncY-决策模型载体.md` 给了
   **6 片 TDD 切片 + 5 条验收标准**。交接稿这句「缺位」是**误判**。

**建议：改文档。** §11.4 那两条改成「已解 + 剩余动作」。

---

## D5. 交接稿 §6.3「独立变异检验」的规则已被 09-16 裁定改写，稿子没同步

- `docs/.../2026-09-13-执行到交付-PRD交接稿.md:561`（§6.3）：
  > **定义**：由**未参与该模块实现**的执行者，**独立地破坏实现**……
- `outputs/2026-09-16-裁定更正与补定-致执行侧.md`（09-16 裁定）：执行者改为
  **「我自测 + owner 复核」**，不再要求「未参与实现的独立执行者」。
- 同稿 `:58` / `:522` / `:631`（R5）三处也都在重复旧规则。

**不一致的表现**：文档要求的执行者，与 owner 后来定的执行者不同。照旧稿做会卡在
「找不到独立执行者」。

**建议：改文档。** §6.3 + `:58` + `:522` + `:631` 四处一并改写，并留一行「原规则
为何被改」（找不到独立执行者）。代码不动。

---

# E 组 · 陈旧文案（低风险，可直接清）

| # | 位置 | 现状 | 事实 |
|---|---|---|---|
| E1 | `hub/web/routes_api.py:30/47/63/78` | 4 条 `/api/items/*/stances` 路由**直接调 `stance_svc`**，没走 `methods.mcp_*` | D3 单一事实源字面不达标（同服务层 + 同契约，精神上达标）。新写的 4 条都走了严的那条路 |
| E2 | `hub/schemas/stance.py:157` | `StanceListItem` docstring 写「私有字段默认清单【**待 owner 确认**】」 | `2026-09-14-裁决答复:47` **已认可**该清单。标记该摘 |
| E3 | `tests/api/test_stance_delayed_disclosure.py:62` | 测试名叫 `test_未终态时成员列表只见本人**加聚合计数**` | 同文件 docstring `:5` 自己写着「**没有「按轮聚合计数」这个东西**」；测试体也只断言「只见本人」。名字是作废的 k=3 方案残留 |
| E4 | `rUdiTJ` 事项原文 | 引用 `hub/llm/client.py:31-32,121` 的分级重试 | 已迁到 `hub/domain/retry.py`。交接稿 §11.3 也登过 |

**建议**：E2 / E3 直接清（纯文案）。E1 要不要回填统一，你上次没答，这里再问一次
（**我倾向不回填**：4 条都工作正常，改了要动 4 处 + 重跑；但要在文档里登记
「这 4 条是历史写法」）。E4 随 `rUdiTJ` 正文更正一起做。

---

# F 组 · 文档引用的文件在仓库里**不存在**（影响可核性）

| 被引用的路径 | 引用方 | 磁盘事实 |
|---|---|---|
| `outputs/2026-09-16-裁定更正与补定-致执行侧.md` | 开工包 README `:14`、`04作废:23` | **不在仓库**（在工作区 `outputs/`，未随仓库走） |
| `outputs/2026-09-16-裁定落地执行记录.md` | `04作废:24` | **不在仓库**（在工作区） |
| `outputs/2026-09-16-波0-完成标准逐条核对.md` | 交接稿 `:770` | **不在仓库**（在工作区） |
| `knowledge/09-六项完成标准核对.md` | 交接稿 `:20` | **不存在** |
| `docs-draft/roR8Pk-隐私边界评估.md` | 交接稿 `:23` | **不存在**（实际在 `docs/superpowers/specs/`） |
| `docs-draft/PRD-v1.2-修订稿.md` | 交接稿 `:144` | **不存在**（实际在 `docs/superpowers/specs/`） |

**为什么这算同类问题**：D1–D5 每一条我都要「照文档核」，而核到一半发现**文档引用的
依据本身不在仓库里** —— 这直接削弱了「文档是单一事实源」这个前提。旁人接手时，
按交接稿 `:23` 去找隐私评估会找不到，进而可能重新评估一遍。

**建议**：把工作区那三份 `2026-09-16-*` 补进仓库 `outputs/`（它们是 09-16 裁定与
落地的一手记录）；交接稿里那三处死路径改指实际位置或标注「未随仓库分发」（E4 同类）。

---

# 汇总：需要你逐条决定的

| # | 一句话 | 我的建议 | 你 |
|---|---|---|---|
| A1 | `get_digest` 缺 `decision`（决议/草案）一节 | **改** | |
| A2 | `get_digest` 没有 `DigestOut` 契约（全仓唯一裸 dict） | **改**（与 A1 一起） | |
| A3 | `StanceRead` 仍直出 `confidence` 原值（PRD-07 未落地） | **改** | |
| B1 | 留痕 detail 多一个 `content_hash` 键 | **维持 + 改文档** | |
| B2 | 「钉死 detail 键集合」的测试不存在 | **补测试** | |
| C1 | 非成员在流水线 403 / 立场层 404 | **维持 + 文档写明分叉** | |
| C2 | `get_summary` 的闸门是实现加的，文档没要求 | **维持 + 回填文档** | |
| D1 | `content_digest` 不匹配的错误码文档自相矛盾 | **改 §15.1** | |
| D2 | FR-20「Agent 无拍板权」未修订 + 悬空引用 | **改文档** | |
| D3 | §9.6.1 契约边界 4+4 与事实 12+11 不符 | **改文档** | |
| D4 | 交接稿 §11.4 两条已过期 | **改文档** | |
| D5 | 交接稿 §6.3 变异检验执行者规则已改 | **改文档** | |
| E1 | 4 条 stances 路由未走 methods | **维持 + 登记** | |
| E2 | `StanceListItem` docstring 的【待 owner 确认】该摘 | **清** | |
| E3 | 测试名残留「聚合计数」 | **清** | |
| E4 | `rUdiTJ` 引用的 `hub/llm/client.py` 路径失效 | **随正文更正一起做** | |
| F1 | 6 个被引用文件不在仓库 | **补 3 份 + 标注 3 处** | |

**记住一条**：`A1`/`A2`/`A3`/`B2` 是要动代码 + 跑门槛 + 进 `main` 的；
`B1(改文档)`/`C1`/`C2`/`D1`–`D5`/`E1(登记)`/`E2`/`E3`/`E4`/`F1` 都**只动文档**，
可以一批做完。

---

# 执行结果（2026-09-17，owner 裁定「冲突一律按最新的来」）

## 落地清单

| # | 处置 | 落点 |
|---|---|---|
| A1 | ✅ **改**：`get_digest` 补 `current_round` / `decision` / `open_items` 三节 | `hub/mcp_server/methods.py::mcp_get_digest` |
| A2 | ✅ **改**：新增 `DigestOut`，出参走 `_contract`（此前是全仓唯一对外裸 dict） | `hub/schemas/mcp_outputs.py` |
| A3 | ✅ **改**：`StanceRead.confidence` → `confidence_band`；新增分档纯函数 | `hub/domain/confidence.py`、`hub/schemas/stance.py` |
| B1 | ✅ **改**（推翻了本件原先的「维持」建议）：留痕 detail 回到**两键**，去掉 `content_hash` —— 口径是**未被推翻的处方**，且 `fact_version` 已是版本标识，冗余 | `hub/api/stances.py` |
| B2 | ✅ **补**：`tests/api/test_audience_view_audit_keys.py`（文档要求的「键集合被测试钉死」**从来不存在**） | 新增 |
| C1 | ✅ **文档化**：非成员 403/404 的分叉**写明是有意的**（不是新裁定，是把既成事实写下来） | PRD v1.2 §9.6.2 |
| C2 | ✅ **文档化**：`get_summary` 读侧 = 事项成员，非成员 404；并注明该闸门是 09-16 补的、此前无闸门 | PRD v1.2 §9.6.2 |
| D1 | ✅ **改文档**：§15.1 的 `422 VALIDATION_FAILED` → `HUMAN_APPROVAL_REQUIRED`（与 §9.3/§9.5/场景 32 一致） | PRD v1.2 §15.1 |
| D2 | ✅ **已核对，实际无缺陷**（见下方更正） | v1.1 FR-20 两处指引 + v1.2 §2.2 #38 |
| D3 | ✅ **改文档**：§9.6.1 契约边界按实测重写（12 端点 / 11 工具），并加「新增接口必须登记」纪律 | PRD v1.2 §9.6.1 |
| D4 | ✅ **改文档**：§11.4 两条标记为已关闭 | 交接稿 §11.4 |
| D5 | ✅ **改文档**：§6.3 变异检验执行者改规则，四处（`:58` / `:522` / `:631` / §6.3）同步 | 交接稿 |
| E1 | ✅ **改代码**（推翻了本件原先的「维持」建议）：新增 `mcp_list_stances` / `mcp_read_stance_analysis`，4 条立场路由改调 methods | `hub/mcp_server/methods.py`、`hub/web/routes_api.py` |
| E2 | ✅ **清**：`StanceListItem` docstring 摘掉【待 owner 确认】；测试里的同名注释一并改 | `hub/schemas/stance.py`、测试 |
| E3 | ✅ **清**：测试名去掉「聚合计数」 | `tests/api/test_stance_delayed_disclosure.py` |
| E4 | ⏳ **登记**：`rUdiTJ` 正文更正需看板工具（当前不可达），与「回填事项正文」同批 | 遗留 |
| F1 | ✅ **补**：三份一手记录补入仓库 `outputs/`；交接稿三处死路径改指实际位置并加更正块 | `outputs/`、交接稿 §0.2 引用表 |

## 对**本件自身**的一处更正（诚实记录）

**D2 我原先写「`PRD12:684` 的『见 §2.4』是悬空引用」—— 说重了。**
核实后：§2.4 **存在**（`#### 2.4 代理承诺开关与两条提交路径`，含 2.4.1–2.4.6），
只是它讲的是**授权开关**，不含 FR-20 拍板修订本身；而 FR-20 的修订正文
**就在 §2.2 变更日志第 38 行**，v1.1 的 FR-20 行也**确实**加了「已被 v1.2 修订」
的批注。所以**没有悬空引用**，只是指针不够精确。已把 v1.1 两处指引改为
「见 v1.2 §2.2 变更日志 #38」。

## 还剩什么

**已全部按「最新口径」处置完毕，无遗留的**口径**分歧。** 只剩两类**非口径**事项：

1. **E4**：`rUdiTJ` 事项正文的回填（口径早已定，缺的是看板工具）。
2. **交接稿 §11.3 #12**：`tests/domain/test_stance_privacy.py` 文件级 docstring
   与用例级 docstring 自相矛盾（全量跑下来是绿的）。属文案，未动。

**另需 owner 复核的两处**（按 09-16 改后的 §6.3 规则，自测结果交你复核）：
- A3 的**取舍**：`confidence` 从单读端点消失后，读得到别人立场的人**再也拿不到
  把握度原值**，只能看到低/中/高。若你认为「能读到立场的人本就该看到依据」，
  这条要回退（那就要同时把 PRD-07 标作废）。
- E1 的**回填**：4 条立场路由现在多套了一层 methods 包装。行为与出参形状不变，
  但多了一层间接。若你认为不值当，可以回退成原来的直调。

## 门槛

全量 **696 passed, 1 skipped**（前值 670 passed, 1 skipped，新增 27 条）；
`ruff` 全绿；`hub.db` 三项指纹零漂移（`f00294f2…444daf9` / 188416 / 13:34:38）。

