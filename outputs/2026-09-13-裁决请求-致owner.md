# 致 owner · 裁决请求（中转站 v1 · 执行到交付）

日期：2026-09-13 ｜ 交付报告：`outputs/2026-09-13-交付报告-接手执行.md`（§9 九节）
当前主线：`origin/main = 7a5398c`（624 passed + 1 xfailed + ruff 全绿）

以下裁决请求按「阻塞程度」排序。**标 🔴 的直接阻塞剩余实现块的开工或收口**；其余不阻塞开发但阻塞整体交付定义 D1。

## 一、🔴 产品口径（ask / decide / digest 三工具的唯一拦路石）

`r5Am9i` 的 7 个立场层工具中，`submit_stance` / `read_stance` / `get_summary` / `declare_item` 已按同源模型实现（`7a5398c`）；剩下三个的语义依赖一份「方案」文档（穷尽渠道未找到，未比对声明见交付报告 §7）：

1. **ask_participant 的配额 N**：roR8Pk 评估要求「按 (matter_id, actor, target) 滑窗配额，超限 429」，N 由配置决定 → **请给出每事项/每日的定向提问上限**。
2. **can_commit 闸门**：decide_item 经 MCP 拍板涉及 S2 闸门三项（agent_authority 已开 / irreversible 不为真 / 授权有效且本人开给自己）与 Q9–Q12 的 irreversible 拉人机制；FR-20 又规定 Agent 无拍板权 → **请裁定 can_commit 下 decide_item 的确切语义（仅 create draft？可终裁？irreversible 事项强制拉人的形态）**。
3. **get_digest 语义**：rpQt6D 命名注意「与 hub/domain/digest.py 区分」，digest 端点的返回内容（事项级决策摘要？包含哪些节？）**无任何可达定义** → 请给一段验收口径。

> 事项原文佐证（roR8Pk 全文，取自 wb-issues todo_show 存档）：对第 2/4/5 条写的是问题而非规格——「confidence……**需定义**是否外发、外发到什么粒度（如分档而非原值）」「ask 配额……**需频次上限**」「ttl 过期后**由谁负责**重新拉人？」。即三者的「是否/多少/由谁」本就是留给 owner 的待答项。
> 另：第 5 条的判定标准要求 `stance_expired` 审计事件——属**新增审计常量**，按红线 R9 需 owner 批准（现仅 `SCHEMA_MIGRATED` 获批）；批准前过期检测/排除可做，审计与重新拉人不可做。

## 二、🔴 roR8Pk 第 1 条：可见性粒度（唯一 strict xfail）

反推用例 `test_public_view_does_not_allow_identifying_stance_changer` 按 PRD §4-C1 登记 xfail。修复需把成员可见粒度从「逐人逐轮直给」收窄，但**与 rrBazW 已冻结的 B22（参与方可互读立场 → 200）正面冲突**（knowledge/08 §4.5-1 已列 owner 裁定）。三个候选：
- A. 聚合计数 + k-匿名阈值（评估文档推荐之一）；
- B. 逐人立场延后到事项终态再公开（评估文档推荐之一）；
- C. 维持现状，把该条改判为「按 A2 口径已达成」（改事项）。

## 三、🟡 §8.4 七项（交接稿原文转述，不阻塞开发）

1. 4.3 两处文案定稿措辞；2. `irreversible` 变更理由是否必填；3. 开通 `can_commit` 是否限非终态事项；4. PRD §1.1 定位句；5. 私有字段清单（`StanceListItem` 5 字段）确认；6. 新增审计常量 `AUDIENCE_VIEW_DELIVERED` 是否批准（批准后 rsEXuh ⑥留痕即可一行落地）；7. faithfulness「逐字等于源事实」完整校验归属。

## 四、🟡 rUdiTJ / rs9ncY：三段不可达，无法按红线开工

逐字核对前提（交接稿 §3.2 强制动作）无法满足，实测如下：

- 会话转录中 `todo_show` 的调用记录仅覆盖 `r5Am9i / r8SwEG / rRSEcS / rfhWa9 / rluUVY / roR8Pk` —— **rUdiTJ / rs9ncY 从未被取过全文**（roR8Pk 全文已在本轮从存档转录中恢复，佐证见上）；
- WorkBuddy 本地库（workbuddy.db / edge-sync）grep 零命中（wb-issues 为远端 MCP 服务，正文在服务端）；
- 本机全部可达存储仅存这两项的**标题**：`rUdiTJ【P1】轮次编排：双环轮询 + 收敛判据（主指标改桥接共识）`、`rs9ncY【P2】决策模型载体 + 从历史决策提炼原则（skill）`。

按交接稿红线 R3（禁止按标题判范围——本项目最贵的教训），**这两项在取得事项原文前不得开工**。解阻塞方式：在可访问 wb-issues 的会话里 `todo_show rUdiTJ` / `todo_show rs9ncY` 并把三段贴入交付报告附录。

## 五、已达成部分速览（详见交付报告 §2–§5）

A1/rfhWa9 全达成（8 条迁移 + 每项可回滚 + 审计守卫）；rrBazW B27 脱敏；rsEXuh faithfulness 校验器；roR8Pk 读审计收口；r5Am9i 的 R1–R10 分级重试 + C2「401 也重试」缺陷修复 + 4 工具 Pydantic 契约 + D1–D6 REST 降级；rRSEcS PRD v1.2 落库。门槛：624 passed + 1 xfailed + ruff 全绿 + hub.db 零污染。
