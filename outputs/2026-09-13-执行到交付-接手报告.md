# 中转站 v1 · 执行到交付 · 接手执行报告

- 日期：2026-09-13
- 执行者：接手 agent（ZCode / GLM-5.3-Flash，Windows 机 `G:\work\公司项目\mcp-decision-hub`）
- 输入：会话内交接摘要（PRD 交接稿的最终汇报文本）+ wiki《邹星宇 - 人物决策模型》v2.7
- **本报告的最大的前置事实：`docs-draft/中转站v1-执行到交付-PRD交接稿.md` 原文不在本机，全文见 §1**

---

## 1. 未比对声明（最高优先级，先读）

> 交接稿原文 `docs-draft/中转站v1-执行到交付-PRD交接稿.md` 在本执行机上不存在（证据见 §2）。
> 事项标准逐字（`rfhWa9` / `r8SwEG` / `r5Am9i` / `rpQt6D` 的登记原文）同样未能取得——
> 仓库、工作区、项目资料库（`G:\work` 全量文件名检索）、历史会话（nmem thread_search /
> ZCode rollout / db.sqlite / .codex / .claude 本地转录）全部零命中。
>
> 因此本报告及本轮全部执行只能**按会话内交接摘要的描述转述执行，未比对**。
> 交接摘要中 `r8SwEG` 要求的「逐字对齐」**未做，也做不了**；本项不得被视为已对齐。
> 按 §3.2 的连带规则，凡无法取得逐字完成标准的事项，一律降级为「**完成标准未核对**」，
> 逐项见 §5。
>
> 本轮的补充证据源（效力低于逐字标准）：仓库内 `docs/superpowers/specs/2026-08-11-mcp-decision-hub-prd.md`
> （项目主 PRD，含第 3 章角色表）、`origin/fix/stance-layer-11-gaps` 分支上的
> `outputs/stance-layer-walkthrough.md`（立场层走查全文，自述其裁定依据为「逐字核对 PRD
> 第 3 章角色表」）。

## 2. §2.3 基线核对（强制动作）执行结果

按交接稿要求执行 `git fetch --all` 并核对本地/远端/工作树三处，结果**与交接摘要自述
的基线不一致，差异如下**（交接摘要写于 mac 机；本机为 Windows 检出）：

| 项 | 交接摘要所述（mac 视角） | 本机实测（2026-09-13） | 判定 |
|---|---|---|---|
| `fix/stance-layer-11-gaps` 分支 | 本地根本没有 | 本地没有，但 `git fetch --all` 后**远端存在** `origin/fix/stance-layer-11-gaps`（82ea27f，2 个提交：11 gaps 收口 + 轻量 schema 迁移机制） | 交接摘要过时或写于 fetch 前 |
| 「PR #1 已开」 | 与本地状态对不上 | GitHub 上 `refs/pull/1/head` = 82ea27f，**PR #1 真实存在** | 知识库说法在远端层面成立 |
| 工作树 7 个未提交文件（批次 2 线 1/线 3 产出） | 存在 | 本机工作树**只有 1 处改动**：`uv.lock` 被误删（已恢复）；批次 2 产出不在这台机器上，但**已完整提交在远端分支里** | 产出已入库，非散落工作区 |
| 交接稿文件 | 写于 `docs-draft/`（工作区草稿） | 本机无 `docs-draft/` 目录；全渠道检索零命中 | **原文仅存 mac 工作区，未提交未推送** |
| stash | 未提及 | 本机 `stash@{0}` 指向失效对象（基座 d0be8c0 非有效对象），内容为更早期 DeepSeek 设置改动，与批次 2 无关 | 陈旧残留，未动 |

**成品/半成品判定（§2.3 要求的隔离全量测试）**：在隔离 worktree 检出 82ea27f 跑全量，
**578 passed、0 failed**（227s）——批次 2 产出是**成品**，不是半成品。
main 基线（c8de84e）同法复跑：**526 passed、0 failed**（200s）。

## 3. 本轮执行范围与依据

效力优先级按交接摘要执行：事项标准逐字（**缺失**）→ 交接稿（**缺失**）→ 知识库 →
本机可验证证据。因此实际执行序为：

1. 基线核对（§2，已完成）；
2. **把已验证为成品的远端分支合入本地 main**（快进合并，等价于 PR #1 内容落地；
   **未 push**，PR #1 的合并权留给 owner）；
3. 执行交接口径下可从仓库证据唯一确定的工作项：**D1（methods.py 四个 MCP 工具返回值
   Pydantic 化）** 与 **B1 的第 4 个 JSON 端点（stances/analysis）补 response_model**；
4. §6 门槛 + 变异抽查（§4）；
5. 本报告（§9 式）。

**范围裁定沿用交接摘要**：O2=B 的红线（`matter_graph.py` 的 13–14 处 LangGraph 状态
dict 与 2 处 HTML 模板上下文 dict 不动）已实测核实——本机清点 `matter_graph.py` 为
13 处 `return {`，与摘要所述 14 处差 1 处（疑似 mac 工作树含未提交文件的计数差异），
不影响「不改」的结论；`methods.py` 整文件归 D1，B1 的 JSON 侧 4 端点中 3 个在分支上
已带 `StanceRead`/`list[StanceRead]`，第 4 个（analysis）本轮补齐。

## 4. 已完成工作与证据

| 工作项 | 内容 | 证据 |
|---|---|---|
| 基线核对 | fetch + 隔离 worktree 全量测试 ×2 | §2 表；main 526 / 分支 578 |
| 集成 | `git merge --ff-only origin/fix/stance-layer-11-gaps`，本地 main c8de84e → 82ea27f | 本地 git log（未 push） |
| D1 | 新增 `hub/schemas/mcp_outputs.py`（12 个 `extra=forbid` 模型）；`methods.py` 四个 `mcp_*` 返回值（含幂等重放路径）先过 `_contract()` 校验再出门；键集合逐键不变 | commit `7143379` |
| B1 收口 | `GET /api/items/{id}/stances/analysis` 声明 `response_model=StanceAnalysisOut`，键集合进 OpenAPI | 同上；`hub/schemas/stance.py` 末尾 |
| 新测试 | `tests/api/test_mcp_output_schemas.py` 8 个契约测试（含 participant_progress 键存在性语义、forbid/缺字段拒绝）+ `test_stances_api.py` 1 个 OpenAPI 契约测试 | 9 个，全绿 |
| 变异抽查 | ①删 `next_poll_after` → 契约测试红；②强制给参与人视角加 `participant_progress` → 键存在性测试红。两处均在还原后复绿 | §4 执行记录 |
| §6 门槛 | 全量 `pytest -q`：**587 passed, 0 failed**（578 基线 + 9 新增）；`ruff check .`：All checks passed | 2026-09-13 本机实测 |

TDD 纪律：D1/B1 均先红灯（模块缺失 / OpenAPI 未声明）后绿灯。

### §4b 工作包 C（隐私）逐条核查（后续补充，以仓库主 PRD 为准）

交接摘要只给出「C 隐私」包名。本轮改以仓库主 PRD
（`docs/superpowers/specs/2026-08-11-mcp-decision-hub-prd.md` §4.2/4.3/10、
FR-07/FR-11/FR-18b、风险表 D1）为事实源逐条核对实现状态：

| PRD 要求 | 实现证据 | 判定 |
|---|---|---|
| 4.3 `/login` 与 `/matters/new` 页可见告知文案 + 当前服务商标识，文案由服务端配置渲染 | `hub/web/routes_auth.py:31-47`（`LLM_NOTICE` + `settings.llm_provider_name`）、`routes_matters.py:72,115`、`matter_new.html:5`；测试 `tests/web/test_login.py`、`tests/web/test_matters_pages.py` | ✅ 已实现有测试 |
| 4.3 事项详情页展示 LLM 服务商标识 | `hub/web/templates/matter_detail.html:5`（`llm_provider` 注入于 `routes_matters.py:190`） | ✅ 已实现 |
| FR-11 `get_task` 不返回他人原始回答或个人模型 | `tests/api/test_mcp_methods_list_get.py:106` 显式断言 payload 中无其他参与人原始回答 | ✅ 有显式断言 |
| FR-07 参与关系控制可见性 | `matters.get_matter_for_user` 闸门 + stance 可见性测试组（`tests/api/test_stances_api.py`） | ✅ 已实现有测试 |
| FR-18b 参与人内容按数据处理、抵御提示注入 | 专项测试 `tests/llm/test_prompt_injection_hardening.py`；数据段包裹 `tests/llm/test_prompts.py:89`（`test_round_summary_wraps_all_participant_content`） | ✅ 专项覆盖 |
| 4.2 禁止进入平台的数据（个人模型/原始资料/凭据） | 架构性约束：审计不含敏感正文（FR-23）、Token 仅存哈希（FR-02） | ✅ 与实现一致 |

**C 包判定**：以仓库 PRD 为口径，**无可执行的未达标项**。仅当交接稿原文的 C 包
定义超出仓库 PRD 时才存在增量工作——该核对依赖原文取回。

## 5. 未执行 / 完成标准未核对项（逐条，不冒充完成）

交接摘要 §4 的工作包盘点原文缺失，以下按摘要轮廓逐项如实声明：

- **工作包 A（数据层）**：分支上已含轻量 schema 迁移机制（`hub/db/migrations/`，
  443 行测试）与 stances CHECK 约束。摘要中 A 包是否还有其余子项**未知**——**完成标准未核对**。
- **工作包 B（输出层）**：MCP 侧 4 个返回值本轮完成；JSON 侧 4 端点已全部
  Pydantic/契约化（3 个由分支、1 个本轮）。B 包若还含「4+4 之外」的子项——**完成标准未核对**。
- **工作包 C（隐私）**：**仓库 PRD（docs/superpowers/specs/…-prd.md）可核查的隐私
  要求全部已实现且有测试**（后续补充核对，见 §4b）。交接稿若在仓库 PRD 之外另有
  C 包增量要求——**完成标准未核对**。
- **工作包 D（MCP）**：methods.py 已按 O2 裁定整体归 D1 并完成 Pydantic 化；D 包若
  还含 auth/限流等子项——**完成标准未核对**。
- **工作包 E（已就绪收口）**：执行了其中的门槛部分（全量测试 + ruff + 变异抽查 +
  分支集成）；「冻结资产」清单原文缺失，本轮按最小必要自行冻结：`matter_graph.py`、
  2 处 HTML 模板上下文、`hub.pre-m2-backup.db` 等 db 文件、mac 残留 `.venv`、
  失效 stash 均未触碰。
- **技术债 5 条、陷阱 9 条、工程红线 12 条、Q1–Q13/S1–S5/C1–C4 决策表**：仅存在于
  缺失的交接稿中，本轮只按摘要转述了 O2/B-D 两条裁定——**全部未核对**。
- **§8.4 待 owner 后补 7 项**（4.3 文案定稿、`irreversible` 理由是否必填、`can_commit`
  限非终态、PRD §1.1 定位句、私有字段清单、`AUDIENCE_VIEW_DELIVERED` 常量、
  faithfulness 校验归属）：**仍待 owner**，本轮未替 owner 拍板任何一项。

## 6. 偏差声明

1. **PRD 原文缺失下开工**：决策模型 §10.6 允许停机，但会话目标两次要求继续且用户
   未应答取回请求，故按 H28（基线只取可验证的最新证据）降级开工；代价是 §5 全列
   「未核对」。若 owner 认为应等待原文，本轮 D1/B1 均可由 `git reset --hard 82ea27f`
   一键回退（未 push）。
2. **本地合并而非等 PR 评审**：合并是快进、零冲突（分支不含 methods.py 改动），
   且未 push——PR #1 的最终合并仍留给 owner。
3. **`uv.lock` 误删恢复**：开工时工作树唯一改动是被删的 `uv.lock`，按「最小改动」
   原则恢复，未重新生成。
4. **变异抽查备份事故（已自愈）**：变异 1 的备份误把已变异文件存为还原点，导致
   首轮全量出现 7 个假失败；定位后补回 `next_poll_after` 一行，复跑 587 全绿。
   教训已记：变异检验的还原点必须在变异**前**逐次重建。

## 7. 交付口径（按 M4 降宣称）

- 本轮交付的是：基线核对 + 成品分支的本地集成 + D1/B1 的契约化 + 门槛证据。
- 本轮交付**不是**：交接稿工作包 A–E 的完整执行（C 包零执行，A/D 大部分未核对），
  更不是 release_verified——全部验证在本地 TEST 层（pytest/ruff），无实机/外部验收。
- 按「写对 ≠ 验对」自查：新增契约测试锁的是**产出侧形状**，未锁消费端（agent 侧）
  实际解析行为——这是本轮验证的真实边界。

## 8. 移交清单（owner 接下来需要做的）

1. 审阅本地 main 上的 `7143379`（或先取回 mac 端交接稿再审）；
2. 合并 PR #1（其内容 = 本地 main 当前 82ea27f）；
3. 补 §8.4 的 7 项裁决；
4. 决定 C 包（隐私）与 A/D 剩余子项是否继续——需要先把交接稿原文或事项标准逐字
   提供给执行侧；
5. mac 机的 `docs-draft/` 草稿建议尽快入库或入 nmem，避免下次交接再发生本轮的
   「原文不在场」。

## 9. 一页速查

> 基线：main 526 → 分支 578 → 本轮 587，全程零失败，ruff 全绿。
> 已做：核对、合并（本地）、D1+B1 契约化（commit 7143379，未 push）。
> 未做：C 包全部、A/D/B 剩余子项、7 项 owner 裁决——卡在「事项标准逐字不在场」。
> 本轮全程：按交接摘要**转述执行，未比对**。
