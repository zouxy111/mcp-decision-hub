# 波 0 · 11 项待做事项「完成标准」逐条核对表

> **性质**：交接稿 §5「推荐时序」波 0 = 「基线核对 + 逐字抄录所有待做事项的完成标准」的产出。
> 同时是交付报告 §2「逐项完成标准核对」的前身。
> **核对对象**：`github/main` @ `2caf8696c01834f01151bc32032599ac526f1ae2`
> **工作副本**：`outputs/.bundlecheck/work`（远端 `github` → `github.com/zouxy111/mcp-decision-hub.git`）
> **核对方式**：从项目事项系统逐字取事项原文（`todo_show`）→ 到代码里逐条定位证据 → 判定。
> **免责**：判定基于**代码与测试文件的存在性 + 静态阅读**；「已达成」项须待全量门槛跑绿才算最终成立（门槛结果见 §4）。

---

## 0. 结论先行

**交接稿 §3.3 的判定已大面积过期。** 交接稿说 10 项是「🟡5 / ❌1 / ⏸1 / ⬜3」；实测 11 项是：

| 判定 | 交接稿 §3.3 | 本次实测 |
|---|---|---|
| ✅ 已达成 | 5（均为 3 项 Skeleton 旧项，不在 10 项内） | **4**（`rfhWa9` `rrBazW` `rsEXuh` `roR8Pk`） |
| 🟡 部分达成 | 5 | **4**（`r8SwEG` `rpQt6D` `r5Am9i` `rRSEcS`） |
| ⬜ 未开始 | 3 | **3 + 1**（`rUdiTJ` `rs9ncY` `r2EOiO`） |

**真实剩余工作量远小于交接稿所述**。11 项里有 4 项可以直接进入「登记完成」，另有 4 项的剩余动作已被本次核对收敛到**可列举的程度**（不再需要"再核一遍"）。

**交接稿自身查出 13 处偏差**（§3），其中 3 处是"标称逐字、实为改写"，1 处是**缺整份分包**（`rpQt6D`），1 处是**同一文件内自相矛盾**。

---

## 1. 判定汇总表

| # | 事项 | 判定 | 一句话依据 | 剩余动作 |
|---|---|---|---|---|
| 1 | `rfhWa9` 迁移机制补齐 | ✅ **已达成（超额）** | `migrations.py` 有 9 组对称 `upgrade`/`downgrade`，覆盖全部 5 张表变更 + v3 枚举 | 无（登记完成） |
| 2 | `r8SwEG` MCP 协议版本核对 | 🟡 部分（已 paused） | 仓库侧版本核对完成；文档无 `protocolVersion` 实测值 | 第 2 条受"方案"文档不在仓库阻塞 → 登记 |
| 3 | `rpQt6D` 云端立场层三表+端点 | 🟡 部分 | schema 层基本达成；**8 个端点只落地 2 个** | 补 6 个端点；**交接稿无分包，需补** |
| 4 | `rrBazW` 立场读取+可见性+审计 | ✅ **已达成** | B22–B27 六条测试全在 `test_stances_api.py` | 无（登记完成） |
| 5 | `rsEXuh` 差异化分发 | ✅ **已达成** | `audience.py` 四步管线 + 三项收口全落地 | 无（登记完成） |
| 6 | `r5Am9i` MCP 接入层 | 🟡 部分 | 7 个工具落地 5 个；重试/降级/幂等齐；验收第 4 条未覆盖 | 补 `ask_participant`/`decide_item` + 1 条测试 |
| 7 | `rRSEcS` PRD 修订 | 🟡 部分 | PRD v1.2 已落库；**验收第 3 条是代码测试，交接稿漏认** | 补 1 条 `can_commit × irreversible` 测试 |
| 8 | `roR8Pk` 隐私边界加固 | ✅ **已达成** | 评估文档 + 反推用例齐；6 条里 5 条收口、1 条已被裁定作废 | 无（登记完成；第 4 条口径按 09-16 裁定更正） |
| 9 | `rUdiTJ` 轮次编排 | ⬜ 未开始 | `stalemate`/`桥接` 全仓零命中 | 需先解 1 处事项间口径冲突 |
| 10 | `rs9ncY` 决策模型载体 | ⬜ 未开始 | `决策日志`/`decision_log` 全仓零命中 | **原文无「完成标准」段**，需补定 |
| 11 | `r2EOiO` ttl 过期拉人提醒 | ⬜ 未开始 | `提醒`/`remind`/`notify` 在 `hub/` 零命中 | 承载形态未定，需先定口径 |

---

## 2. 逐项明细

### 2.1 `rfhWa9` · 数据库迁移机制补齐 —— ✅ 已达成（超额）

**完成标准（事项原文逐字）**
> 能回答“改既有表时怎么安全改、怎么回滚”。

⚠️ **交接稿 §4 A1 把它写成**：「补迁移机制 + **5 张既有表的结构变更** + **每项可单独回滚**」——
这**不是**原文的完成标准，而是把原文「做什么」第 1、2 条压缩重述。交接稿标注「逐字」，**实为改写**（偏差 #1）。

**代码证据**

`hub/db/migrations/migrations.py`（509 行）—— 9 组 `upgrade`/`downgrade` **全部对称存在**：

| 版本 | upgrade | downgrade | 对应事项要求 |
|---|---|---|---|
| v1 baseline | `:81` | `:90` | — |
| v2 add_stance_check_constraints | `:94` | `:172` | — |
| **v3** stance_authority_enum_and_approval | `:311` | `:337` | `stances.authority` 收敛为枚举 + `approved_at` |
| v4 add_matter_item_columns | `:371` | `:377` | `matters` +4 列 |
| v5 add_participant_authority_columns | `:391` | `:397` | `matter_participants` +2 列 |
| v6 add_summary_evaluation_columns | `:412` | `:418` | `round_summaries` +2 列 |
| v7 rescope_idempotency_primary_key | `:447` | `:467` | `idempotency_records` 改主键 |
| v8 add_output_authority | `:500` | `:506` | `outputs` +authority |

其余证据：
- `SCHEMA_MIGRATED` 审计常量 —— `hub/api/audit.py:40`（注释：补充决策 A，owner 已批）
- 迁移 CHECK 约束 —— `hub/db/migrations/migrations.py:258`
- 模型侧同步 —— `hub/db/models.py:243`（authority CHECK）、`:269`（`authority_legacy` 注释）
- 测试 —— `tests/db/test_schema_evolution.py`（含 `:158` 断言 4 列、`:206` 断言降级后列消失）
- `tests/db/test_schema_evolution.py:322` 唯一 skip（本机无真实 `hub.db` 实例）

**判定**：**已达成，且超出原文要求**。原文只要求"能回答怎么安全改、怎么回滚"，实际交付了 9 组可回滚迁移 + 审计 + 测试。

**剩余动作**：无。登记完成。

---

### 2.2 `r8SwEG` · MCP 协议版本核对 —— 🟡 部分达成（事项已 paused）

**完成标准（事项原文逐字）**
> 文档里出现明确的 protocolVersion 实测值 + “主路径为第 3.4 节”的结论。

**代码证据**
- `grep -rn "2025-11-25" docs/` → **零命中**。文档里**没有**任何 `protocolVersion` 实测值。
- `.venv/lib/python3.13/site-packages/mcp/types.py:27` → `LATEST_PROTOCOL_VERSION = "2025-11-25"`。
  ⚠️ 这是**依赖库的声明值**，不是事项要求的「**实测宿主实际协商到的** protocolVersion」。
- 事项状态 = **paused**，`pause_reason` 原文：
  > 剩余两项需要当前不具备的外部条件：① 实测宿主（WorkBuddy / Claude Code）实际协商到的 protocolVersion ② 把方案第 3.3/3.4 节对调主次——但「方案」文档不在本仓库。仓库侧核对已完成（见评论）。

**判定**：**部分达成**。第 1 条（实测值）与第 2 条（对调主次）**均未达成**，且第 2 条受 §7.3 已登记的「『方案』文档缺失」硬阻塞。

**⚠️ 与交接稿口径不一致**：交接稿 §3.3 把本项判为「🟡 部分达成」，但事项**自身状态已是 `paused`**。两者应择一登记。

**剩余动作**：第 1 条可做（需宿主配合，属外部条件）；第 2 条做不了 → 按 §7.3 登记「未比对」。

---

### 2.3 `rpQt6D` · 云端立场层三表 + 端点 —— 🟡 部分达成

**完成标准**：⚠️ **事项原文没有「完成标准」段**。可核的是「硬约束（用户明确要求）」：
> - 每个模型用 Pydantic v2 表达，禁止裸 dict 出入参
> - 所有 endpoint 的 request / response body 必须是 Pydantic 模型
> - 枚举用 StrEnum / Literal，confidence 加 ge=0 le=1，非法枚举必须 422 拒绝且不落库
> - 每个模型补一条测试，证明非法输入被 schema 拦下
> - 这三张表同时是 MCP 工具 inputSchema 的单一事实源
> - 字段级脱敏落在 schema 定义上

⚠️ **交接稿 §4 没有本项的分包**（偏差 #4）——§3.3 盘点含 `rpQt6D`，§4 只给 9 个分包，缺这一项。而按 §3.2 `:157`，它是范围最大的一项。

**代码证据（schema 层：已达成）**
- `hub/schemas/stance.py` —— 5 个 `StrEnum`（`StanceKind:17` / `DisagreementKind:25` / `ActingAs:32` / `Urgency:37` / `Visibility:43`）
- `StanceCreate`（`:57`）：`extra="forbid"`（`:63`）、`confidence: float = Field(ge=0.0, le=1.0)`（`:67`）、`content_hash` 正则（`:83`）
- `StanceRead`（`:86`）、`StanceAnalysisRead`（`:120`，含 `model_validator` faithfulness 校验 `:140`）、`StanceListItem`（`:154`，B27 字段裁剪）
- `hub/schemas/mcp_outputs.py`（MCP inputSchema 单一事实源）
- 测试：`tests/schemas/test_stance_schema.py`、`tests/api/test_stances_api.py:714 test_非法载荷返回422且不落库`

**代码证据（端点层：**只落地 2/8**）**

现有 —— `hub/web/routes_api.py`：
| 端点 | 行号 | 状态 |
|---|---|---|
| `POST /api/items/{id}/stances` | `:23` | ✅ 对应要求 #3 |
| `GET /api/items/{id}/stances/analysis` | `:40` | （不在原文 8 个列表内，是附加） |
| `GET /api/items/{id}/stances/{user_id}` | `:56` | ✅ 对应要求 #4 |
| `GET /api/items/{id}/stances` | `:71` | ✅ 对应要求 #2 的一半（列表） |

原文要求的 8 个端点、缺失的 6 个：
| # | 要求端点 | 状态 |
|---|---|---|
| 1 | `POST /items` | ❌ 缺 |
| 2 | `GET /items?participant=me&state=open` | ❌ 缺 |
| 3 | `POST /items/{id}/stances` | ✅ `routes_api.py:23` |
| 4 | `GET /items/{id}/stances/{participant_id}` | ✅ `routes_api.py:56` |
| 5 | `GET /items/{id}/summary` | ❌ 缺（有 `analysis`，语义不同） |
| 6 | `POST /items/{id}/ask` | ❌ 缺 |
| 7 | `POST /items/{id}/decide` | ❌ 缺 |
| 8 | `GET /items/{id}/digest?participant=me` | ❌ 缺 |

⚠️ 交接稿 §3.2 `:157` 说「仓库只落地 **1 载体 / 1 端点 / 1/3 Schema**」——实测是 **2 个端点**（偏差 #9）。

**判定**：**部分达成**。schema 层 ≈ 达成；端点层 2/8。

**剩余动作**：补 6 个端点（其中 `ask` 与 `decide` 与 `r5Am9i` 的缺 2 个 MCP 工具同源）；**并在交接稿里补本项分包**，明确与 B1/B2 的重叠边界。

---

### 2.4 `rrBazW` · 立场读取 + 参与方可见性权限 + 读取审计 —— ✅ 已达成

**完成标准（事项原文）**：原文给了 6 条 TDD 行为清单 B22–B27，即完成标准本身。

**代码证据 —— 六条测试全部存在于 `tests/api/test_stances_api.py`**

| 条目 | 要求 | 测试 |
|---|---|---|
| B22 | 参与方 A 能读参与方 B 的立场 → 200 | `:153 test_参与方能读到另一参与方提交的立场` |
| B23 | 非参与方 C 读任何立场 → 404 | `:173 test_非参与方读取立场返回404` |
| B24 | 读不存在的 user_id → 404 | `:192 test_读取不存在的参与人立场返回404` |
| B25 | 跨事项隔离 → 404 | `:204 test_跨事项读取立场返回404` |
| B26 | 读取记审计（复用 `AuditEvent`，不改表） | `:224 test_读取立场写入审计事件` |
| B27 | 列表端点不含私有字段 + 按 visibility 过滤 | `:274 test_单人读取同样按可见性过滤`、`:330 test_立场列表只返回本事项且按可见性过滤` |

实现侧：
- `hub/api/stances.py:255-270 _visible()`（可见性过滤）
- `hub/schemas/stance.py:154-180 StanceListItem`（B27 字段裁剪）
- `hub/api/audit.py:35 STANCE_READ`

**判定**：**已达成**。

**剩余动作**：无。登记完成。

---

### 2.5 `rsEXuh` · 差异化分发 —— ✅ 已达成

**完成标准**：⚠️ **事项原文没有「完成标准」段**。原文给的是「硬要求」+「schema 校验要求」+「反模式」。
原文硬要求：
> - 裁剪必须由代码执行，不能写在提示词里靠模型自觉
> - 不是二选一：确定性裁剪是主防线，出口扫描是兜底，两者都要
> - 所有视图投影自同一份 canonical fact（CQRS read model）
> - 每个视图携带事实版本号 / 内容哈希，生成后做 faithfulness 校验
> - 异议方单独一条消息（含 ① 你的意见是什么 ② 为什么没被采纳 ③ 什么条件下会重新考虑）
> - 保真转述，不得假装反对

⚠️ **交接稿 §4 B1 把它写成**：「立场层的产出可被下游消费 + **留痕** + **faithfulness 校验** + **全链路 Pydantic（明写「禁止裸 dict」）**」
——这是对原文硬要求第 3、4 条的**改写**，不是逐字摘录（偏差 #2）。且 B1 的标题「立场层交付化收口」与原文标题「差异化分发」**语义不符**。

**代码证据 —— 管线四步全落地**

`hub/domain/audience.py`（185 行，纯函数零 IO）：
| 步骤 | 要求 | 实现 |
|---|---|---|
| ① canonical fact | 结构化事实基座 | `FactBase` `:26-42`（含 `fact_version` / `decision` / `rationale` / `supporting_user_ids` / `opposing_user_ids` / `risks` / `action_items` / `dissenting`） |
| ② 确定性裁剪（主防线） | 代码执行，非提示词 | `build_audience_view` `:61-97` |
| ③ 消息组装 | 按接收者 | 同上（`sections` 字典） |
| ④ 出口扫描（兜底） | 扫 ③ 的产物 | `scan_view` `:139-185` |

异议方消息 3 项齐 —— `audience.py:76-86`（「你当初的意见」/「为什么这次没有采纳」/「什么情况下会重新考虑」）。
扫描局限如实暴露 —— `SCAN_LIMITATIONS` `:123-128`（"扫描说没发现问题，不等于没有问题"）。

`hub/api/stances.py`：
- `check_faithfulness` `:391-445`（逐字校验 + `FaithfulnessError:448`），调用点 `:210`
- 留痕 ⑥ `AUDIENCE_VIEW_DELIVERED` `:212-224`，detail = `{viewer_user_id, fact_version, content_hash}`
  ⚠️ 交接稿 B1 写 detail = `{viewer_user_id, fact_version}`，**实际含 `content_hash`**（偏差 #6）
- `hub/api/audit.py:43` 常量已存在

测试：`tests/domain/test_audience.py`（含 `:110` 出口扫描拦截用例）。

**判定**：**已达成**。管线 + 三项收口（Pydantic 化 / 留痕 / faithfulness）全部落地。

**剩余动作**：无。登记完成。

---

### 2.6 `r5Am9i` · MCP 接入层 —— 🟡 部分达成

**完成标准（事项原文「=== 验收 ===」段）**
> - 一条测试覆盖「同一 idempotency_key 重复提交 只落一条 + 返回首次结果」
> - 一条测试覆盖「4xx 不触发重试」
> - 一条测试覆盖 REST 降级路径返回结构一致
> - 一条测试覆盖「irreversible=true 时 can_commit 代理提交被拒」

⚠️ **交接稿 §4 D1 把它写成**：「见 `docs-draft/r5Am9i-剩余两项-验收标准草案.md` 的 R1–R10 / D1–D6」
——**路径、文件名、状态三处都不对**。实际文件是 `docs/superpowers/specs/2026-09-13-r5Am9i-验收标准-定稿.md`（在 `specs/` 不在 `docs-draft/`，且已"定稿"不是"草案"）（偏差 #3）。
另：该文档 H1 仍写「验收标准**草案**」，与文件名「定稿」及正文 §四「已定稿（2026-09-13 owner 拍板）」冲突。

**代码证据**

7 个要求工具，**已落地 5 个**：
| 工具 | 行号 |
|---|---|
| `mcp_declare_item` | `hub/mcp_server/methods.py:499` |
| `mcp_submit_stance` | `:543` |
| `mcp_read_stance` | `:562` |
| `mcp_get_summary` | `:576` |
| `mcp_get_digest` | `:602` |
| **`ask_participant`** | ❌ 缺 |
| **`decide_item`** | ❌ 缺 |

其余能力：
- 重试分级 —— `hub/domain/retry.py`（65 行，`should_retry:36` / `backoff_delays:47`，注释含 R1–R5/R7/C1/C4）+ 测试 `tests/llm/test_client_retry.py`
  ⚠️ 交接稿 §4 D1 写「（`hub/domain/retry.py`，**新建**）」——**已存在**（偏差 #8）
- REST 降级 D1–D6 —— `tests/api/test_rest_fallback.py:52/60/87/110/118/122`（**六条全覆盖**）+ `:156 幂等键跨通道通用`
- 幂等 —— `hub/domain/idempotency.py` + `tests/domain/test_idempotency.py`
- Pydantic 契约辅助 —— `hub/mcp_server/methods.py:57 _contract`

**验收四条逐条核**：
| # | 要求 | 状态 |
|---|---|---|
| 1 | 幂等重复提交只落一条 | ✅ `tests/domain/test_idempotency.py` |
| 2 | 4xx 不触发重试 | ✅ `tests/llm/test_client_retry.py`（R1） |
| 3 | REST 降级返回结构一致 | ✅ `tests/api/test_rest_fallback.py:60`（D2） |
| 4 | **irreversible=true 时 can_commit 代理提交被拒** | ❌ **未找到对应测试** |

关于第 4 条，现有的是**另外两件事**（不是同一条）：
- `hub/api/resolutions.py:71-77` —— irreversible 事项不允许 Agent **终裁**（`decide_resolution`，非 `submit_stance`）
- `tests/api/test_rulings_2026_09_14.py:58` —— can_commit 开通限**非终态事项**（裁决 5b）

**判定**：**部分达成**。

**剩余动作**：① 补 `ask_participant` / `decide_item` 两个工具；② 补验收第 4 条测试。

---

### 2.7 `rRSEcS` · 修订 PRD 2.1 人审声明 —— 🟡 部分达成

**完成标准（事项原文「=== 验收 ===」段）**
> - PRD 修订稿落到 docs/superpowers/specs/ 下，版本号递增
> - r5Am9i 的权限边界实现与该修订保持一致
> - 一条测试：irreversible=true 时，agent_authority=can_commit 的代理提交被拒

⚠️ **交接稿 §4 E 把它写成**：「剩余为 **PRD 落库**（`docs/superpowers/specs/`，版本号递增）」
——**漏认了第 3 条**。第 3 条是**代码测试**，不是文档动作（偏差 #7、与 §2.6 验收第 4 条**是同一条**）。

**代码证据**
| # | 要求 | 状态 |
|---|---|---|
| 1 | PRD 修订稿落 `docs/superpowers/specs/`，版本号递增 | ✅ `docs/superpowers/specs/2026-09-13-mcp-decision-hub-prd-v1.2.md` 存在（`2caf869` 内又补了 §2.2 变更日志与 §4 回填） |
| 2 | 与 r5Am9i 权限边界一致 | 🟡 部分 —— `hub/api/matters.py:348-360`（can_commit 限非终态，裁决 5b）、`hub/db/models.py:243`（CHECK）、`hub/schemas/stance.py:79`（Literal）已落；`can_commit × irreversible` 闸门未见 |
| 3 | 一条 `irreversible × can_commit` 拒绝测试 | ❌ 未找到 |

**判定**：**部分达成**。第 1 条达成；第 2 条部分；第 3 条未达成。

**剩余动作**：与 §2.6 剩余动作 ② 是同一条测试，做完即关闭两项。

---

### 2.8 `roR8Pk` · 隐私边界加固 —— ✅ 已达成

**完成标准（事项原文逐字）**
> 交付：一份隐私边界评估 + 一条可测的反推用例（构造 3 人 2 轮局，验证能否从公开摘要反推出谁改了立场）。

✅ 交接稿 §4 C1 的表述与此**一致**（本项是 11 项里少数表述正确的）。

**代码证据**
| 交付物 | 证据 |
|---|---|
| 隐私边界评估文档 | `docs/superpowers/specs/2026-09-13-roR8Pk-隐私边界评估.md`（6 项逐条 + `文件:行号`） |
| 可测反推用例 | `tests/domain/test_stance_privacy.py:110 test_public_view_does_not_allow_identifying_stance_changer`（3 人 2 轮局，`_build_three_person_two_round_matter:58`） |

6 条失效结论的收口状态：
| # | 项 | 收口证据 |
|---|---|---|
| 1 | 跨轮聚合反推 | `hub/api/stances.py:255-270 _visible()` —— 非终态时成员只见本人立场 |
| 2 | confidence 泄露面 | `hub/schemas/stance.py:154-180 StanceListItem` 不含 `confidence`/`rationale_summary`/`non_negotiables`/`conditions`/`disagreement_kind` |
| 3 | 读取侧审计 | `hub/api/stances.py:346-350`（`get_stance`）、`:367-371`（`list_stances`，`scope="list"`） |
| 4 | ask 配额 | ⛔ **已被 2026-09-16 owner 裁定作废**（改为不设配额） |
| 5 | ttl 执行主体 | 过期剔除 + `STANCE_EXPIRED`（`hub/api/audit.py:46`）；**「谁重新拉人」移交 `r2EOiO`** |
| 6 | XPIA 提示注入 | `hub/domain/audience.py:139-185 scan_view` + `tests/llm/test_prompt_injection_hardening.py` |

⚠️ **交接稿 §4 C1 的「4 处附带发现」第 1 条已过期**：它说「`_visible()` **恒 True**（`stances.py:210-223`）→ visibility 成假旋钮」。实测 `_visible()` 在 `:255-270`，**按发起人 / 终态 / 本人三档过滤**，不再是恒 True（偏差 #5）。

⚠️ **文件内自相矛盾（偏差 #12）**：`tests/domain/test_stance_privacy.py` 文件级 docstring `:6-10` 说该用例「**按设计为红**」，而用例级 docstring `:114-116` 说「**断言转绿**。原 xfail 登记摘除」。同一文件两个相反说明。

**判定**：**已达成**。第 4 条被裁定删除（非本项缺陷），第 5 条下半段移交 `r2EOiO`。

**剩余动作**：① 登记完成；② 修 `test_stance_privacy.py` 文件级 docstring 的自相矛盾。

---

### 2.9 `rUdiTJ` · 轮次编排 —— ⬜ 未开始

**完成标准（事项原文逐字）**
> 验收：一条测试证明“无新增信息时不进下一轮、直接判僵持”。

⚠️ **交接稿 §4 D2 写**：「见事项描述三段 → 接手方必须先逐字抄录（**本稿未核对**）」——但原文**有**明确的验收条文（上引）。"未核对"不成立（偏差 #10）。

**代码证据**
- `grep stalemate` / `grep 桥接` 在 `hub/`、`tests/` → **零命中**
- `hub/domain/convergence.py`（29 行，LLM 四态字符串校验）+ `convergence_eval.py`（276 行）为既有实现，未见「无新增信息判僵持」入口

**⚠️ 事项间口径冲突（偏差 #11）**
| 事项 | 原文表述 | updated_at |
|---|---|---|
| `rUdiTJ` | 「授权边界 V1 **锁死 propose_only**。can_commit 与 P0 人审声明互斥（见 r5Am9i），**移出 V1**」 | 2026-09-11 04:15:27 |
| `r5Am9i` | 「**can_commit 开放**，不再锁死 propose_only」（2026-09-11 owner 已决策） | 2026-09-11 04:30:13 |

`r5Am9i` 的更新时间**晚 15 分钟**，且 `rRSEcS` 是它的配套文档修订 → `rUdiTJ` 的「锁死」是**过期描述**。但**两事项未被正式更正**，按 §3.2 连带规则需登记。

**判定**：**未开始**。

**剩余动作**：① 先解口径冲突（建议按 `r5Am9i` 为准，更正 `rUdiTJ`）；② 实现「无新增信息判僵持」+ 1 条测试。

---

### 2.10 `rs9ncY` · 决策模型载体 + 提炼原则 skill —— ⬜ 未开始

**完成标准**：⚠️ **事项原文没有「完成标准」段**，只有「做什么」4 条 + 「待定项」。

⚠️ **交接稿 §4 C2 写**：「见事项描述「做什么 / 验收 / 完成标准」三段 → 接手方开工前必须先逐字抄录（本稿未核对，不得凭本稿推断）」——原文**没有这三段里的"验收"和"完成标准"**（偏差 #10 同源）。**本项的完成标准本身缺位**，属**待定项**，不是"未核"。

**代码证据**
- `grep decision_log` / `grep 决策日志` 在 `hub/`、`tests/` → **零命中**

**原文自带的待定项（需先确认再动手）**
> 本机现有 7 个 memory 技能实为 Nowledge Mem（nmem）的 CLI 壳，数据在远端服务，spaces 项目隔离已存在但 enabled=false。接通记忆这一层时需先定：走 nmem CLI 还是直读本地记忆文件。

**判定**：**未开始**。

**剩余动作**：① 补定完成标准（原文缺）；② 解「nmem CLI vs 直读本地记忆文件」取舍。

---

### 2.11 `r2EOiO` · ttl 过期后的拉人提醒 —— ⬜ 未开始

**完成标准（事项原文「=== 验收 ===」段）**
> - 一条测试证明：立场过期 → 发起人收到提醒
> - 一条测试证明：系统**不**自动换人（过期后事项参与人不变）
> - 非发起人侧不可见该提醒

**代码证据**
- `grep 提醒` / `grep remind` / `grep notify` 在 `hub/` → **零命中**
- 保留部分已核实存在：过期剔除（`hub/api/stances.py:189-195` 的 `skipped` 逻辑）+ `STANCE_EXPIRED` 审计（`hub/api/audit.py:46`）+ 测试 `tests/domain/test_stance_privacy.py:188 test_过期立场不进收敛判定且写审计`
- 「不自动换人」已成立：`hub/api/stances.py` 无 `list_reassignable_expired`（`d1b9400` 已删）

**原文自带的未定项**
> 提醒的承载形态未定：页面内提示 / 事项状态变化 / 待办列表 / MCP 可读字段 —— 建议执行侧先出方案再实现。

**判定**：**未开始**。

**剩余动作**：① 先定承载形态；② 实现提醒 + 3 条验收测试。

---

## 3. 交接稿偏差清单（本次核对查出 13 处）

| # | 位置 | 交接稿写的 | 实测 | 性质 |
|---|---|---|---|---|
| 1 | §4 A1 完成标准 | 「补迁移机制 + 5 张表变更 + 每项可单独回滚」（标"逐字"） | 原文完成标准是「能回答"改既有表时怎么安全改、怎么回滚"」 | 「逐字」实为改写 |
| 2 | §4 B1 完成标准 | 「立场层产出可被下游消费 + 留痕 + faithfulness + 全链路 Pydantic」（标"逐字"） | 原文给的是「硬要求」6 条 + schema 校验要求，无「完成标准」段 | 同上；且标题语义与原文不符 |
| 3 | §4 D1 完成标准 | 指向 `docs-draft/r5Am9i-剩余两项-验收标准草案.md` | 实际 `docs/superpowers/specs/2026-09-13-r5Am9i-验收标准-定稿.md` | 路径/文件名/状态三处不符 |
| 4 | §4 工作包 | 9 个分包 | §3.3 盘点 10 项；**`rpQt6D` 无分包** | **缺整份分包** |
| 5 | §4 C1 附带发现 1 | 「`_visible()` 恒 True（`stances.py:210-223`）」 | `_visible()` 在 `:255-270`，按发起人/终态/本人三档过滤 | 已过期 |
| 6 | §4 B1 留痕 detail | `{viewer_user_id, fact_version}` | 实际含 `content_hash`（`stances.py:220-222`） | 已过期 |
| 7 | §4 E `rRSEcS` | 「剩余为 PRD 落库」 | 验收第 3 条是**代码测试**（`irreversible × can_commit`），被漏认 | 漏认验收条款 |
| 8 | §4 D1 | `hub/domain/retry.py`「新建」 | 已存在（65 行，含 R1–R5/R7/C1/C4） | 已过期 |
| 9 | §3.2 :157 | 「仓库只落地 1 载体 / 1 端点 / 1/3 Schema」 | 实测 2 个端点（`routes_api.py:23`、`:56`） | 计数偏保守 |
| 10 | §4 C2 / D2 | `rs9ncY` / `rUdiTJ`「完成标准未核」 | `rUdiTJ` 原文**有**验收条文；`rs9ncY` 原文**没有**完成标准段 | 「未核」定性不准 |
| 11 | 事项间 | —— | `rUdiTJ`「锁死 propose_only」 vs `r5Am9i`「can_commit 开放」**互相矛盾** | 事项未更正 |
| 12 | `tests/domain/test_stance_privacy.py` | —— | 文件级 docstring 说「按设计为红」，用例级说「转绿」 | **同文件自相矛盾** |
| 13 | §4 D1 文件所有权 | 含 `tests/mcp/` | 目录不存在（MCP 测试在 `tests/api/test_mcp_*.py`） | 路径不存在 |

另：`docs/superpowers/specs/2026-09-13-r5Am9i-验收标准-定稿.md` 的 H1 仍写「**草案**」，与文件名「定稿」及正文 §四「已定稿」冲突。

---

## 4. 门槛（Definition of Done §6.1 口径）

**实测汇总 @ `2caf8696c01834f01151bc32032599ac526f1ae2`**

| 项 | 命令 | 结果 |
|---|---|---|
| 全量测试 | `DATABASE_URL=sqlite:///<隔离目录>/hub.db .venv/bin/python -m pytest tests -q --basetemp=<隔离目录>` | **639 passed, 1 skipped, 2 warnings in 78.19s** |
| lint | `.venv/bin/ruff check .` | **All checks passed!** |
| 无污染 | `hub.db` 指纹三项对比 | **三项完全一致**（见下） |

唯一 skip = `tests/db/test_schema_evolution.py:322`（本机无真实 `hub.db` 实例，给真实库升级用的用例，属预期）。

**指纹对比（门槛前后逐项）**

| 项 | 门槛前 | 门槛后 | 一致? |
|---|---|---|---|
| `hub.db` sha256 | `f00294f21e7729a2cf2ac14b946b6c9f974a9f039f0cc3f1600f5d658444daf9` | 同左 | ✅ |
| `hub.db` size | 188416 | 188416 | ✅ |
| `hub.db` mtime | 2026-09-16 13:34:38 | 2026-09-16 13:34:38 | ✅ |
| `backups/` 条目 | `hub-backup-20260916-133331.db` | 同左（无新增） | ✅ |
| `git status`（排除 `.pytest-*`） | 干净 | 干净 | ✅ |

> 隔离目录里**真的生成了** `hub.db` + 备份文件 → 同时证明"没污染"与"迁移机制真的跑了、不是被跳过"（§6.1 附带收益）。

**⚠️ 一条附带证据**：`tests/domain/test_stance_privacy.py:110` 的用例在全量里**是绿的**（639 passed 不含 failure）。这**实证**了该文件级 docstring :6-10「按设计为红」的说法是错的、用例级 docstring :114-116「断言转绿」是对的（见 §3 偏差 #12）。

---

## 5. 未比对声明

1. 本表的「事项原文」取自项目事项系统 `todo_show`（2026-09-16 当次返回），**未与项目资料库里的文档版本逐字比对**。
2. 「已达成」判定基于**代码与测试文件的静态阅读 + 测试存在性**；**未逐条实跑**每一项的对应测试（全量门槛覆盖整体，不单独定位）。
3. 交接稿 §7.3 的两条未比对声明**依然成立**：① 完成标准引用自 `knowledge/09`，未对全部 15 项逐字重读；② `r8SwEG` / `rRSEcS` 要求的仓库外「方案」文档**穷尽可达渠道后仍未找到**。
4. 本表**不构成根因判断**——判定的是"完成标准是否达成"，不是"为什么没达成"。

---

## 6. 下一步建议

按依赖与收益排序：

1. **`rpQt6D` 补分包**（最高价值）：它是唯一"整项无分包"的，且范围最大（8 端点 + 全量 Pydantic）。需先划清它与 B1/B2 的重叠边界。
2. **一条测试关两项**：补 `irreversible=true × can_commit 代理提交被拒` 的测试，同时满足 `r5Am9i` 验收第 4 条与 `rRSEcS` 验收第 3 条。
3. **补 2 个 MCP 工具**：`ask_participant` / `decide_item`（与 `rpQt6D` 的 `ask` / `decide` 端点同源）。
4. **解 `rUdiTJ` 口径冲突**：先更正「锁死 propose_only」的过期表述，再实现"无新增信息判僵持"。
5. **`rs9ncY` 补定完成标准**：原文缺，属产品口径，需 owner 或 lead 定。
6. **`r2EOiO` 定承载形态**：原文自标未定，动工前必先定。
7. **登记 4 项完成**：`rfhWa9` / `rrBazW` / `rsEXuh` / `roR8Pk`。
