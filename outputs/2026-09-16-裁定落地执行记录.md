# 裁定落地执行记录（2026-09-16）

> 执行人：警长 ｜ 依据：owner 2026-09-16 三条裁定
> 代码：远端 `main = d1b9400e343b0efdb1e483a9ba616c163e9a0aea`

---

## 一、推送阻塞解除

| 时点 | 远端 `main` |
|---|---|
| 执行前 | `649be9c`（停在 09-14） |
| 执行后 | **`d1b9400`** |

两个提交已落地：

| SHA | 内容 |
|---|---|
| `873ff8d` | 09-14 的 5 项裁决落地（原本地提交，未推） |
| `d1b9400` | 本次裁定回退（新提交） |

**通道根因**：git credential helper 为 `!/opt/homebrew/bin/gh auth git-credential`；进程 PATH 缺 `/opt/homebrew/bin` 时 `gh` 不可执行 → 取不到凭据 → 推送失败。补 PATH 后一次通过。

```bash
PATH="/opt/homebrew/bin:$PATH" GIT_TERMINAL_PROMPT=0 \
  git push github <sha>:refs/heads/main
```

> 09-15 那两轮 48 次失败的**具体原因无法从证据反推**，本条只声明一条已验证可用的通路。

---

## 二、裁定 1 · ask 配额 → 不设上限（回退）

| 文件 | 动作 | 规模 |
|---|---|---|
| `hub/domain/rate_limit.py` | 删 `ASK_QUOTA_LIMIT = 100` + `rate_limit_key_ask()` | −10 |
| `hub/api/stances.py` | 删 `create_stance` 配额校验块 + `_ask_limiter` + `RateLimiter` 导入 | −14 |
| `tests/api/test_rulings_2026_09_14.py` | 删 `test_ask配额超限返回429并写审计` | −35 |

### TDD 记录

| 阶段 | 命令 | 观察 |
|---|---|---|
| RED | `pytest tests/api/test_rulings_2026_09_16.py` | `AssertionError: 定向提问被拒：[(101, 429, {'error_code': 'RATE_LIMITED', ...})]` |
| GREEN | 同上，改代码后 | `7 passed` |

测试：`test_定向提问不设配额_超过历史阈值仍可提交` —— 同一 `(matter, actor, target)` 经 HTTP 公共接口连提 **101 次**（= 旧阈值 + 1），全部 `201`。

---

## 三、裁定 3 · ttl 过期 → 删重分配池

| 文件 | 动作 | 规模 |
|---|---|---|
| `hub/api/stances.py` | 删 `list_reassignable_expired()`（全仓无生产调用方） | −17 |
| `tests/api/test_rulings_2026_09_14.py` | 删 `test_ttl过期立场进重分配池` | −31 |

### TDD 记录

| 阶段 | 观察 |
|---|---|
| RED | `AssertionError: 重分配池出口仍在 … assert not True` |
| GREEN | `7 passed` |

测试：`test_ttl过期不产生重分配池出口` —— 退场守卫，钉住被裁掉的出口不再回来（项目已有同类先例：`tests/web_ui/test_scaffold_cleanup.py`）。

**保留不动**：过期立场从 `analyze_round` 剔除 + `STANCE_EXPIRED` 审计（`649be9c` 已落地）。
**新增实现**（发起人拉人 + 系统提醒）→ 事项 `r2EOiO`，**本轮未做**。

---

## 四、测试条数变化

| 文件 | 前 | 后 |
|---|---|---|
| `tests/api/test_rulings_2026_09_14.py` | 7 | **5** |
| `tests/api/test_rulings_2026_09_16.py`（新） | 0 | **2** |
| 合计 | 7 | 7 |

> 更正：09-16 13:08 留言写「由 7 条变 6 条」只算了裁定 1；裁定 1+3 合计为 **7 → 5**。

---

## 五、门槛

**`639 passed + 1 skipped @ d1b9400`** + ruff 全绿

- 基线（改前）：`639 passed + 1 skipped @ 873ff8d`，同为 639/1
- 唯一 skip：`tests/db/test_schema_evolution.py:322` —— 本机无真实 `hub.db` 实例
- 口径：**skip 不计入 passed**。`873ff8d` 提交说明的「640 passed」应写 `639 passed + 1 skipped`

### `hub.db` 零污染（交接稿 §6.1 指纹法）

```
before: sha256=f00294f21e7729a2cf2ac14b946b6c9f974a9f039f0cc3f1600f5d658444daf9  size=188416  mtime=13:34:38
 after: sha256=f00294f21e7729a2cf2ac14b946b6c9f974a9f039f0cc3f1600f5d658444daf9  size=188416  mtime=13:34:38
```

三项一致。`git status` 在此**不可作证据**：`hub.db` 与 `backups/` 均在 `.gitignore` 内，恒干净。

---

## 六、环境坑（可复现，建议写进验收步骤）

复测时 8 条集成测试红，**非项目问题**：

| 项 | 实际装成 | `uv.lock` 锁定 |
|---|---|---|
| `fastmcp` | 4.0.4 | **2.14.7** |
| `mcp` | 2.2.0 | **1.30.0** |

按 `pyproject.toml` 的 `fastmcp>=2.10,<3` 用裸 `pip install` 会越界装到 4.x，MCP 工具派发行为不同 → `MCPError: Not Found`。

**修法**：`uv sync --frozen --no-install-project`。
**结论**：复现门槛必须按 `uv.lock` 装，不能按 pyproject 的版本范围装。

---

## 七、未做（明账）

1. ~~**文档动作 ①②**~~ → **已于第二轮完成**，见 §九
2. **`r2EOiO`** —— ttl 过期发起人提醒，承载形态未定
3. **10 个待做事项的完成标准逐条核对**
4. 未下载 `2026-09-14-回填登记`、`09-13-交付报告-接手执行`

---

## 八、已同步（第一轮）

| 载体 | ID |
|---|---|
| 项目留言板（进度同步） | `msg_3ad0f9e3669a4224a9b2f6e2c1b2dc28` |
| 事项评论 `roR8Pk` | `cmt_245132b9bcf54aec95a9bd44de2bc70a` |
| 事项评论 `r2EOiO` | `cmt_7481f1f0b41444ad9f5e5a95ad42ef0d` |

---

# 第二轮：文档动作 ①②（2026-09-16 傍晚）

> 远端 `main = 2caf8696c01834f01151bc32032599ac526f1ae2`

## 九、FR-20 补指向

| 位置 | 动作 |
|---|---|
| PRD v1.2 §2.2（**本次新增**一节） | 变更日志 **#38**：FR-20 语义修订 → 见 §2.4 |
| PRD v1.1:236 | FR-20 行加注「⚠️ 已被 v1.2 修订，`can_commit` 为显式例外」 |
| PRD v1.1:677 | 追溯矩阵同一行同步加注 |

按 owner 要求**只加注**：v1.1 条款正文一字未动。

## 十、PRD v1.2 §4 回填

- §4.1 拆为 **4.1.1**（09-13 关闭）/ **4.1.2**（09-14、09-16 自 §4.2 移入）
- 移入：#1 前半、#4 前半、#5、#6，另核出 #3、#7 实为已收
- 头部「定稿」补范围说明：**仅指条款正文**，§4.2 待确认项不计入
- 摘除 §1.4 正文两处【待 owner 定稿】；**§0.5 R4 与 §2.1 #30 历史记录行未改**
- 顺带修一处漏网：§1.4「以下两段为草案…定稿前不得作为对外承诺发布」（措辞已定稿）

### ⚠️ 自我更正：§4.2 真正仍开的是 4 条，不是 2 条

第一轮汇报说「#1/#4/#5/#6 已被答完，只剩 #2 与 #8」——**不准确**。
§4.2 的 **#1 与 #4 各含两个问句**，09-14 答复只答了前半：

| 条目 | 前半（已答） | 后半（**未答，仍开**） |
|---|---|---|
| 原 #1 | 4.3 两处文案措辞 → 定稿 | 是否还需「**我已阅读**」勾选确认 |
| 原 #4 | `irreversible` 变更理由 → 必填 | 撤销 `can_commit` 后是否需 Web 标注「授权已撤销」 |

依据：09-14 §8.4-1 只讲措辞（其引用的交接稿 §8.2-S4 本身写作「出草案，**措辞**待 owner 定稿」）；§8.4-2 只讲理由必填。

已拆为 **#1b / #4b** 留在 §4.2。**§4.2 现共 4 条开口：#1b、#2、#4b、#8。**

## 十一、作废开工包 04（不删档）

`outputs/2026-09-14-开工包/04-roR8Pk-第1条-k3聚合计数.md`
→ **`【已作废】04-roR8Pk-第1条-k3聚合计数.md`**（`git mv`，历史可追）

顶部加作废声明 + README 索引 #04 行与 09-16 更正说明同步。核过的事实：

| 项 | 事实 |
|---|---|
| 第 1 条口径 | 按**事项状态**（进行中只见本人 / 终态后全员可见），**不按人数** |
| 落地位置 | `649be9c` → `hub/api/stances.py::_visible()` |
| 聚合计数实现 | **不存在**（全仓无 `per_person_visible` / `k3` 判定） |
| 待做代码 | **无** |

另核过 **01 / 05 无需改动**：

- **01** 标题本就是「定向提问（**无配额**）」→ `873ff8d` 落地的 ask 配额 100 **违背了自己的开工单**
- **05** 已写明「owner 未额外裁定 → 不做重新拉人」，与裁定 3 一致

## 十二、清 3 处口径与事实残留

| 位置 | 残留 | 处理 |
|---|---|---|
| `tests/api/test_stance_delayed_disclosure.py` | 文档串称「只见本人 **+ 按轮聚合计数**」 | 改为按事项状态口径 + **显式声明无计数载荷** |
| `outputs/stance-layer-walkthrough.md:204` | 「本仓库无迁移工具」 | 改为可核实理由 |
| `tests/api/test_stances_api.py:611` | 同上 | 同上 |

**「无迁移工具」的正确说法**：迁移靠 `INSERT INTO stances_new SELECT … FROM stances` 重建表；老库若含未知 stance 行，会被新表 CHECK 拒绝、**整个迁移回滚**（`hub/db/migrations/migrations.py:123-125` 的 `raise`），库遂停留在无约束状态。

> 附带发现：PRD 旧 §4.2 #7 只点名 `models.py:197` / `stances.py:154`（**早已修好**）；同一错误主张当时还活在它没点名的另外两处。

## 十三、门槛（第二轮）

**`639 passed + 1 skipped @ 2caf869`** + ruff 全绿 + `hub.db` 零污染（sha256 一致）

## 十四、已同步（第二轮）

| 载体 | ID |
|---|---|
| 留言板回复（挂第一轮进度留言下） | `msg_5150fbb63e194d3398ef063c380c81c8` |
| 事项评论 `rRSEcS` | `cmt_4770db82c52349969cdf6e476fa27c38` |
| 事项评论 `roR8Pk` | `cmt_34a26d616c294aa4bb289c63effa2f7e` |
